#include <stdio.h>

/**if(a = -3) would give us a warning cuz its not a comparison rather we are simply reassigming a. so the output for this would be -3 is now -3
 *
 */

int main()
{
	int a;
	a = 5;
	//if(a=-3)

	if(a == -3);
	{
	printf("%d is not equal  to:\n", a);
	}
	return 0;
}
