#include <stdio.h>

/**A code to simply get the sizeof a data type 
 *
 */

int main()
{
	int i;
	double d;
	char c;

	printf(" the size of of i is: %lu\n", sizeof(i));
	printf("the size if d is %lu\n", sizeof(d));
	printf("the size of c is %lu\n", sizeof(c));

	return 0;
}
